# --- Contenu confidentiel : ne jamais publier -------------------------------
# Garde-fou : ces dossiers ont été retirés du dépôt. S'ils sont recréés
# localement (analyses réelles, documents internes), ils resteront ignorés.
aipd/
sources/

# --- Outillage local ---------------------------------------------------------
.claude/

# --- Dépendances de génération (docx npm pour word-templates/) : non versionnées
node_modules/

# --- Documents de travail (audits, notes, brouillons) : non publiés ----------
travaux/

# --- Lanceur local (association .rae, chemins machine) : non versionné --------
launcher/

# --- Fichiers temporaires / système -----------------------------------------
*.tmp
*.bak
*~
Thumbs.db
desktop.ini
.DS_Store
