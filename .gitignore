# --- Contenu confidentiel : ne jamais publier -------------------------------
# S'ils sont recréés localement (analyses réelles, documents internes),
# ces dossiers restent ignorés.
aipd/
sources/

# --- Outillage local & dépendances de génération ----------------------------
.claude/
node_modules/

# --- Fichiers temporaires / système -----------------------------------------
*.tmp
*.bak
*~
Thumbs.db
desktop.ini
.DS_Store

# Note : le dossier travaux/ (documents de travail, lanceur local) ignore
# lui-même son contenu via travaux/.gitignore.
